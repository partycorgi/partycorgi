// const defaultTheme = require("tailwindcss/defaultTheme");

module.exports = {
  mode: 'jit',
  purge: ['./src/**/*.js'],
  theme: {},
  variants: {},
  plugins: [
    // require("@tailwindcss/ui")
  ],
}
